Bug fixes and UX improvements for license activation.

- **Fix:** License key was not persisted across app restarts due to a Keychain storage issue. Activating a license now reliably survives relaunches.
- **Fix:** Invalid license keys that passed initial activation but failed server validation are now rejected immediately with a clear error message.
- **Improvement:** License activation now shows a success confirmation instead of silently closing the dialog.
